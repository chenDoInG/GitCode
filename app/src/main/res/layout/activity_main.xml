<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:local="http://schemas.android.com/apk/res-auto"
    >

    <include
        android:id="@+id/activity_main_user_indicator"
        layout="@layout/view_login_user"
        android:layout_gravity="center"
        tools:ignore="IncludeLayoutParam"/>

    <include
        android:id="@+id/activity_main_error_view"
        layout="@layout/view_error"
        android:layout_gravity="center"
        tools:ignore="IncludeLayoutParam"
        />

    <android.support.v4.widget.DrawerLayout
        android:id="@+id/layout_menu"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <!-- The main content view -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            >

            <android.support.v7.widget.Toolbar
                android:id="@+id/activity_main_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/colorPrimary"
                local:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
                local:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                />

            <android.support.v4.widget.SwipeRefreshLayout
                android:id="@+id/activity_main_swiperefresh"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <android.support.v7.widget.RecyclerView
                    android:id="@+id/activity_main_recycler"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="0.90"
                    android:clipToPadding="false"
                    tools:listitem="@layout/item_event"
                    />
            </android.support.v4.widget.SwipeRefreshLayout>
        </LinearLayout>

        <!-- The navigation drawer -->
        <android.support.design.widget.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="240dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            local:headerLayout="@layout/nav_head"
            local:menu="@menu/drawer_view"
            local:itemTextColor="@color/menu_state_list"
            local:itemIconTint="@color/colorPrimaryDark"
            >
        </android.support.design.widget.NavigationView>
    </android.support.v4.widget.DrawerLayout>

</merge>